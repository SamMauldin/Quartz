print("Now changing password for " .. _G["user"])
print("New password:")
local pass = sha.sha256(read("*"))
print("Confirm:")
local cpass = sha.sha256(read("*"))
if pass == cpass then
	file.write("/Home/" .. _G["user"] .. "/.passwd", pass)
	print("Done!")
else
	print("Passwords do not match")
end